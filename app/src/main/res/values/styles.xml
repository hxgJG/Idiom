<resources>

    <style name="AppTheme" parent="IdiomTheme" />

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/loading_splash_bg</item>
    </style>

    <!--侧滑返回-->
    <style name="Custom.SwipeBack.Transparent.Theme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- 统一Activity进出场动画 -->
    <style name="ActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_enter_from_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_no_animation</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_out_to_right</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_no_animation</item>
    </style>

    <!--上下进出场动画-->
    <style name="AppActivityUpDownAnimationStyle" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_bottom_in_300</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_no_animation</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_no_animation</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_no_animation</item>
    </style>

    <style name="ScrollTextView">
        <item name="android:paddingEnd">10dp</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbarThumbVertical">@drawable/update_version_scrollbar_bg</item>
        <item name="android:scrollbars">vertical</item>
    </style>
</resources>
